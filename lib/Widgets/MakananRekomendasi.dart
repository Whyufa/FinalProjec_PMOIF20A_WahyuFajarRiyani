import 'package:flutter/material.dart';

class MakananRekomendasi extends StatelessWidget {
  const MakananRekomendasi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'INI HALAMAN REKOMENDASI MAKANAN',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
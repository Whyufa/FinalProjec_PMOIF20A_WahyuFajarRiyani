import 'package:flutter/material.dart';
class putra extends StatefulWidget {
  const putra({Key? key}) : super(key: key);

  @override
  State<putra> createState() => _putraState();
}

class _putraState extends State<putra> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
